---
version: '3'

services:
    api:
        container_name: thavalon-api
        build:
            context: .
            dockerfile: Dockerfile.local
        ports:
        - "127.0.0.1:8001:8001"
        volumes:
        - .:/app